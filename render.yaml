services:
  - type: web
    name: unicorn-schedule
    runtime: docker
    plan: free
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: DB_PATH
        value: /data/schedule.db
